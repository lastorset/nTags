#/bin/sh
cd ..

# Create output directory
if test ! -e package/output;
then
	mkdir package/output;
fi

VERSION=`cat ./version | sed -e "s/ /_/"`

# Package
tar --exclude-from=package/package-exclude -czf package/output/ntags-${VERSION}.tar.gz *
